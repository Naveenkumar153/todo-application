import { ShowhidepasswordDirective } from './showhidepassword.directive';

describe('ShowhidepasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowhidepasswordDirective();
    expect(directive).toBeTruthy();
  });
});
